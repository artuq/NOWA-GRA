# Tech Debt Register

- **2026-06-23 — RESOLVED 2026-06-23** (ActionSystem Core, Story 001 — real bug, not advisory, already fixed): ADR-0004's testing note suggested driving `get_progress()`'s mid-run test by directly setting `_timer.time_left`. Verified false in Godot 4.6.2/4.6.3 — `Timer.time_left` is a read-only/engine-computed property (`SCRIPT ERROR: Cannot set value into property "time_left" ... because it is read-only`). Fixed by letting the real Timer run for real with a short bounded duration (0.6s) and `await get_tree().create_timer(...).timeout` between checks, instead of faking `time_left`. This is a small real-time wait (~0.5s total), not a violation of "no time-dependent assertions" in spirit — the assertion is on the progress *formula*, not on wall-clock timing; only the test's setup mechanism needs real engine ticks since `Timer.time_left` cannot be faked. — tracked from `production/epics/action-system/story-001-action-core-timer-concurrency.md`
- **2026-07-01** (Locked Slot Preview, Story 005): ADR-0007 originally specified `disabled=true` for locked slots; Story 005 changes to `disabled=false` + a locked tap handler (no `start_action()` call). The safety guarantee — no Autoload mutation on locked-slot tap — is identical, just via a different mechanism. If locked slots ever need to become truly non-interactive again, revert to `disabled=true` and remove the handler. — tracked from `production/epics/action-ui/story-005-locked-slot-preview.md`
- **2026-07-01** (Locked Slot Preview, Story 005): Toast Label implemented as a `PRESET_FULL_RECT` direct child of the slot Button (non-Container). Functional for a 2-second ephemeral overlay; autowrap works because the label fills the button rect. Upgrade to a `CanvasLayer`-based or dedicated overlay-panel toast in the Juice/Feedback System epic if toast styling needs to evolve (shadow, background panel, animation). — tracked from `production/epics/action-ui/story-005-locked-slot-preview.md`
- **2026-07-01** (Sponsor Network Shield, Story 008): AC-8 test (`test_offline_sim_two_segment_drain`) asserts qualitatively that shielded final Morale > unshielded final Morale, but does not verify the exact 2-segment boundary or match the analytic calculation specified in the QA test case. A bug that applies buffer=8 for the full offline window (not just the shielded segment) would still pass this test. Follow-up: add a second call with shield=0 and a third with shield > offline_delta to triangulate the boundary. — tracked from `production/epics/resource-system/story-008-sponsor-shield.md`
- **2026-07-01** (Sponsor Network Shield, Story 008): Three minor QA coverage gaps: (1) no over-delta test asserting `_shield_remaining_seconds >= 0.0` after `_process(999.0)`; (2) no test for rejection when shield is already active and Sponsors are insufficient (confirming active timer is unchanged); (3) no test for `simulate_offline(delta < shield_remaining)` edge case (full-window shielded, segment 2 zero iterations). All non-blocking — covered by construction (`maxf(0.0,...)` clamp; early-return guard; `clampi(shield, 0, remaining)`) but the tests don't prove it. — tracked from `production/epics/resource-system/story-008-sponsor-shield.md`
- **2026-06-23 — RESOLVED 2026-06-23** (ActionSystem Core, Story 001 — real bug, not advisory, already fixed): `GdUnitSignalAssert` has no `is_count()` method (confirmed against the real GdUnit4 v6.2.0-rc1 API in `addons/gdUnit4/src/GdUnitSignalAssert.gd` — only `is_emitted`/`is_not_emitted`/`is_signal_exists` exist). The "signal fires exactly once" test was rewritten to count emissions via a directly-connected counter callable instead. Note: GDScript lambdas capture outer locals **by value**, not by reference — the counter must be a single-element `Array` (or other reference type), not a plain `int`, or the count silently stays at 0. — tracked from `production/epics/action-system/story-001-action-core-timer-concurrency.md`

- **2026-06-23 — ROADMAP/DECISION FLAG (not debt yet)** (Platform / Rendering): If CrazyGames (web/HTML5) becomes a release target, it forces an architecture decision currently unmade. Godot 4 web export requires the **Compatibility (GL Compatibility) renderer** — Forward+/Mobile do not run in-browser. `technical-preferences.md` currently specifies Forward+/mobile for Android. Compatibility runs on Android too, so the clean move is to standardize on Compatibility for web+mobile (our 2D game needs nothing more). Also needed for CrazyGames: `crazysdk-godot-4` addon (Full Launch only), Brotli/Gzip-compressed production builds, COOP/COEP threading verification, and a check that ADR-0002 (atomic save) + Offline Progress behave under web's `user://`→IndexedDB mapping. NONE of this blocks current backend work (pure GDScript, renderer-agnostic). Decide + write an ADR when the rendering/UI layer is reached, not before (YAGNI). Verified feasible 2026-06-23 (CrazyGames officially supports Godot 4: docs.crazygames.com/sdk/intro).

- **2026-06-23 — RESOLVED 2026-06-24** (Core Resource Mutation & Clamping): Production code uses English resource keys (Reach/Cringe/Haters/Morale/Sponsors), but `design/gdd/resource-system.md` and `design/registry/entities.yaml` still document the original Polish keys (Zasięgi/Hatersi/Sponsorzy) — docs now stale relative to code. **Fixed (Sprint 5, Story 5-1)**: `entities.yaml`'s 3 stale `name:` fields renamed to the English code keys, with a new `display_name_pl:` field preserving the Polish term for narrative reference; `resource-system.md`'s Resource Table got a new Code Key column and a note explaining the resolution. Narrative prose elsewhere in `resource-system.md` intentionally left in Polish — translating the full document was out of scope for this fix (the risk was key-identifier drift, not language). — tracked from `production/epics/resource-system/story-001-core-resource-mutation.md`
- **2026-06-23** (Core Resource Mutation & Clamping): `_CLAMPED_KEYS` in `src/core/resource_manager.gd` is a hardcoded constant array, not loaded from a data file. Defensible for now (clamping rules are architecturally load-bearing, not live-tunable balance data) — revisit if future stories need per-resource bounds beyond a binary 0-100 clamp. — tracked from `production/epics/resource-system/story-001-core-resource-mutation.md`
- **2026-06-23** (Hatersi Passive Growth Rate, Formula A): `H_EXP` in `src/core/resource_formulas.gd` must stay a whole number — the "always finite, never NaN" guarantee for out-of-contract negative Cringe input relies on `pow(negative_base, H_EXP)` resolving via integer-exponent math. Documented inline in the constant's doc comment; if `H_EXP` is ever tuned to a non-integer value (safe range per `resource-system.md` is [1.5, 2.5]), re-verify `test_out_of_contract_negative_cringe_returns_finite_value()` still passes. — tracked from `production/epics/resource-system/story-002-haters-growth-rate.md`
- **2026-06-23** (MAJOR, all Resource System stories): the project had **no `project.godot`** at the repo root, **no installed GdUnit4 addon**, and `tests/gdunit4_runner.gd` referenced a nonexistent `GdUnitRunner.gd` class — a fictional API that doesn't match real GdUnit4 (v6.2.0-rc1). This meant none of Stories 001/002/003's tests had ever actually been executed by a real Godot engine, despite earlier session confirmations of "tests passed locally." Fixed during Story 003's closure: created a minimal `project.godot`, installed GdUnit4 to `addons/gdUnit4/`, corrected the runner script, added `.gitignore` for `.godot/`/`reports/`. Running the suite for real then surfaced two additional real bugs (see entries below) — both fixed. All 21 tests across Stories 001-003 now genuinely verified passing. — tracked from `production/epics/resource-system/story-003-morale-drain-rate.md`
- **2026-06-23** (Core Resource Mutation & Clamping — real bug, not advisory, already fixed): `tests/unit/resource_system/core_mutation_test.gd` passed bare `{}` dictionary literals directly to `apply_delta(deltas: Dictionary[StringName, float])`. Godot 4.4+'s typed-dictionary parameter matching against inline literals is inconsistent/non-deterministic in this Godot version (4.6.2) — some calls were accepted, others rejected with "element type" mismatch, depending on context, not on the literal's shape. Fixed by declaring an explicitly-typed local `Dictionary[StringName, float]` variable before every `apply_delta()` call site. Production code's typed signature is correct and unchanged. — tracked from `production/epics/resource-system/story-003-morale-drain-rate.md`
- **2026-06-23** (Core Resource Mutation & Clamping — real bug, not advisory, already fixed): `core_mutation_test.gd`'s `after_test()` called `_rm.queue_free()` unconditionally, but GdUnit4's own GC (`GdUnitMemoryObserver`) can free tree-added nodes between test stages when a test uses `await` (e.g. `assert_signal`), causing a double-free crash. Fixed with an `is_instance_valid(_rm)` guard before `queue_free()`. — tracked from `production/epics/resource-system/story-003-morale-drain-rate.md`
- **2026-06-23 — RESOLVED 2026-06-23** (Cringe Delta Clamping / Sponsors, Stories 1-6 & 1-7): both stories' `/story-done` director gates (QL-TEST-COVERAGE via `qa-lead`, LP-CODE-REVIEW via `lead-programmer`) initially could not be spawned — persistent HTTP 500s on the Task/Agent path; reviews were done inline instead. **Root cause diagnosed** via a 4-spawn differential test: it was NOT a plugin defect and NOT a general subagent outage — it was a transient server-side incident on the **sonnet model specifically** (built-in `Explore` on opus = OK; ccgs `community-manager` on haiku = OK; ccgs `qa-lead` on sonnet = 500; same `qa-lead` with `model: haiku` override = OK). Most ccgs agents pin `model: sonnet`, so they were the ones caught. **Resolution**: re-ran all three deferred gates by spawning the real ccgs subagents with a `model: opus` override (the Agent tool's model param takes precedence over agent frontmatter), bypassing the broken sonnet path — 1-6 QL-TEST-COVERAGE = ADEQUATE, 1-6 LP-CODE-REVIEW = APPROVE, 1-7 LP review = APPROVE. Independent audit trail now exists; no further action. Workaround for future use while sonnet is degraded: pass `model: opus` (or `haiku`) when spawning ccgs agents. — tracked from `production/epics/resource-system/story-006-cringe-clamping.md` and `story-007-sponsors-acquisition.md`
- **2026-06-23** (Passive Zasięgi/Reach Income, Formula D): `passive_zasiegi_income()`'s "zero cross-calls to other ResourceFormulas methods" acceptance criterion is currently verified only by a documentation-only test (`test_code_inspection_no_cross_call_to_effectiveness_multiplier`, asserts `true`) — a future refactor that introduces a cross-call would not be caught automatically. Recommended fix (per `lead-programmer`'s LP-CODE-REVIEW review): write a static-analysis/lint check once (e.g. grep-based CI step) that fails if any `ResourceFormulas` static function calls another `ResourceFormulas` static function, applied across all formulas (A-E), rather than patching per-story. Not blocking — low risk while each formula body stays a single-line pure expression. — tracked from `production/epics/resource-system/story-005-passive-income.md`
- **2026-06-24** (Path Resolution Algorithm, Story 002): `_REGISTERED_PATHS` in `src/core/history_flag_manager.gd` is a hardcoded `const Array[Dictionary]` (currently 2 entries: Pato-Streamer Hazardowy, Guru-Celebryta), not loaded from a data file — flagged by LP-CODE-REVIEW as drifting from coding-standards.md's "configuration values loaded from data files" rule. Acceptable at Foundation tier with only 2 paths and no consumers yet; revisit once Decision Card System / Class Path System exist and the path count grows past 2-3, since at that point designers will want to retune/add paths without touching code. — tracked from `production/epics/history-flag-system/story-002-path-resolution-algorithm.md`
- **2026-06-24** (Debounce/Coalescing & Mobile Lifecycle Flush, Story 002): ADR-0002's Key Interfaces section specifies `save_now()` emits a `save_flushed` signal on successful save (after the atomic rename commits), but `src/core/save_system.gd`'s implementation omits it — flagged by LP-CODE-REVIEW as ADR drift. Not yet needed since no consumer exists; add it (and the corresponding emit call) when a future story needs to react to save completion (e.g., a "saving..." UI indicator, or Offline Progress System confirming a save landed before computing Δt). — tracked from `production/epics/save-persistence-system/story-002-debounce-lifecycle-flush.md`
- **2026-06-24** (MVP Card Content, Story 001): the GDD's own MVP content table violates its own stated AC-7/Tuning-Knob 1.4x-1.8x Reach risky:safe ratio bound for 3 of 8 pairs — `staged_drama` (220/120 = 1.833), `leaked_dm` (190/105 = 1.810), `cancel_threat` (200/110 = 1.818). Implemented as-authored (the 3 outliers read as intentional, higher-stakes/milestone-bearing cards); the unit test locks the real measured ratios rather than the abstract bound. Revisit if/when card balance is formally re-tuned — either accept the wider range as the real Tuning Knob, or adjust these 3 cards' numbers to comply. — tracked from `production/epics/card-content-database/story-001-mvp-card-content.md`
- **2026-06-24** (MVP Card Content, Story 001): the GDD's AC-13 ("Sponsors key only appears on `sponsor_offer_shady` and `brand_deal_choice`") contradicted the GDD's own neutral-card table, which gives `fan_in_trouble` a `Sponsors -1.0` cost on both options. Corrected: the qualifying-card gate (positive Sponsors *reward*) stays restricted to the 2 cards; `fan_in_trouble`'s flat cost is a separate, allowed case. `design/gdd/card-content-database.md`'s AC-13 wording should be updated to match this corrected rule next time that GDD is revised. — tracked from `production/epics/card-content-database/story-001-mvp-card-content.md`
- **2026-06-24** (MVP Card Content, Story 001 — flagged by LP-CODE-REVIEW): `src/core/card_content_database.gd`'s `CARDS` const uses `StringName` resource keys (`&"Reach"`, `&"Cringe"`, `&"Morale"`, `&"Sponsors"`) with no compile-time or test-time link to `ResourceManager`'s actual key set (`src/core/resource_manager.gd`'s `_resources` dict) — a future independent rename in either file would silently desync them. Recommended fix: add an integration test asserting every resource key referenced in `CardContentDatabase.CARDS` exists in `ResourceManager`'s known resource set, converting silent drift into a loud test failure. Not blocking at 12 cards / 2 modules; revisit once Decision Card System (the actual consumer) is built. — tracked from `production/epics/card-content-database/story-001-mvp-card-content.md`
- **2026-06-24** (Decision Card System epic close, flagged by LP-CODE-REVIEW): `docs/architecture/adr-0005-decision-card-weighting-cooldown.md`'s Implementation Guidelines code sample is now doubly stale — Story 001 already flagged the Dictionary-vs-Resource mismatch (annotated in the ADR), and Story 003 added a second drift (the sample never shows `_check_pool()`'s wiring to `present_next_card()`). The ADR is accurate in spirit but every code sample in it is now wrong; `src/core/decision_card_system.gd`'s own header/method doc comments carry the real documentation burden. Recommend a follow-up pass to either replace ADR-0005's code samples entirely with a pointer to the source file, or formally revise the ADR via `/architecture-decision`. — tracked from `production/epics/decision-card-system/story-003-presentation-resolution.md`
- **2026-06-24** (Decision Card System epic close, flagged by LP-CODE-REVIEW): `DecisionCardSystem._card_intensity()` identifies a card's "risky" option by checking which option's `counter_increments` contains `&"risky_choices_count"` — a soft, implicit coupling to Card Content Database's schema convention rather than an explicit field. Works correctly for all 12 MVP cards (exactly one risky/safe pair pattern). Revisit if Card Content Database's schema ever adds a second counter that could appear alongside or instead of `risky_choices_count` on a card's options. — tracked from `production/epics/decision-card-system/story-003-presentation-resolution.md`
- **2026-06-24** (Decision Card System epic close, flagged by LP-CODE-REVIEW): Test-only seams (`cards_override` on `_check_pool()`/`_build_eligible_pool()`, `set_seed()` on the RNG) have accumulated on `DecisionCardSystem`'s public/semi-public surface across all 3 stories. Acceptable per this codebase's established convention (same pattern as `ResourceManager`, `ActionSystem`, `HistoryFlagManager`), but worth revisiting if Card UI or Class Path System begin calling into `DecisionCardSystem` directly — consider whether these seams need tighter visibility scoping at that point. — tracked from `production/epics/decision-card-system/story-003-presentation-resolution.md`
- **2026-06-24** (Action UI, Story 001 — real gotcha, not a bug, documented for future stories): adding a new `class_name`-declared script (`src/ui/action_ui_formatting.gd`) caused the test runner to fail with "Identifier not declared in the current scope" and crash, because Godot's global script class cache (`.godot/global_script_class_cache.cfg`) only updates after an editor pass — the headless `gdUnit4` runner alone doesn't trigger one. Fixed by running `godot --headless --path . --import` once after adding the new class, before re-running tests. Recommendation: run this import step as a standing habit any time a brand-new `class_name` script is introduced (not needed for edits to existing classes, only first-time additions). — tracked from `production/epics/action-ui/story-001-number-formatting-progress-bar-math.md`
- **2026-06-24** (Action UI, Story 002 — process decision, not a defect): UI-type stories' Test Evidence allows either a manual evidence doc OR an automated interaction test. Since this agent session has no way to actually view a rendered Godot scene, manual evidence docs would be unverifiable claims. Resolved: use GdUnit4's `scene_runner()` (headless scene instantiation + node/property assertions) as the standing evidence method for all UI-type stories in this project, not just Story 002. Confirmed working — `scene_runner()` successfully instantiates `.tscn` files headlessly and exposes child nodes via `find_child()`/`.scene()`. Apply this same approach to Stories 003/004 and any future UI work. — tracked from `production/epics/action-ui/story-002-resource-hud.md`
- **2026-06-24 — RESOLVED 2026-06-24** (Action UI, Story 004 — real flaky test, already fixed): `GdUnitSceneRunner.simulate_frames(1)` advances real engine frame timing (not a fixed deterministic delta), which intermittently desynced a `_process()`-driven label assertion in `running_action_overlay_interaction_test.gd` — observed as a confusing diff where expected/actual strings appeared visually identical but the assertion still failed (likely a sub-frame timing race affecting `ActionSystem.get_progress()`'s float math at the exact instant of comparison). Fixed by using `runner.invoke("_process", 0.0)` to call `_process()` directly with an explicit, deterministic delta instead of `simulate_frames()`, for tests asserting exact label text after exactly one `_process()` tick. `simulate_frames()` remains appropriate for tests only checking coarse-grained behavior (e.g. "value changed" or "still > some threshold"), not exact-string assertions. — tracked from `production/epics/action-ui/story-004-running-action-overlay.md`
- **2026-06-25** (Action UI redesign — pre-release obligation, not a bug): icons in `assets/ui/icons/` (Eye/Chili/Skull/Gear/Cash/Microphone/Potion/Letter/Locked) are sourced from "Icons Essential" v1.2 by Crusenho Agus Hennihuno, CC BY 4.0 — free to use commercially but requires attribution. See `assets/ui/icons/ATTRIBUTION.md`. Must be added to the game's in-game credits screen before any public release; no credits screen exists yet. — tracked from Action UI redesign, Sprint 6 follow-up
- **2026-06-25** (Action UI redesign — pre-release courtesy, not a blocker): the global UI font is VT323 (SIL OFL 1.1, see `assets/ui/fonts/ATTRIBUTION.md`). OFL allows embedding/commercial use freely; a credits-screen mention is courtesy, not legally required for embedded use. Bundle alongside the icon-pack CC BY attribution when a credits screen exists. — tracked from Action UI redesign, Sprint 6 follow-up
- **2026-06-26** (Card UI, Story 002 — GDD-vs-implementation gap, documented not blocked): card-ui.md's modal AC calls for a "category icon (sponsor/drama/hater/neutral)" + situation text + option labels, but `CardContentDatabase.CARDS` has NO `category` field, and all `text`/`label` fields are empty placeholders (narrative copy authoring is a separate, unscheduled task per that GDD's Open Questions). CardScreen falls back to: card `id` as the title when `text` is empty, neutral "← Option A"/"Option B →" when labels are empty, and a single generic placeholder category icon (Book.png) since no category data exists. When narrative copy + a category taxonomy are authored, (1) populate text/labels in CardContentDatabase, (2) add a `category` field + per-category icons, (3) update CardScreen._populate() to map category→icon. — tracked from `production/epics/card-ui/story-002-card-screen-modal.md`
- **2026-06-26** (Card UI — partial resolution of the prior entry): the empty `text`/`label` placeholder copy is now AUTHORED (12 cards, English satirical copy). Still outstanding from that entry: no `category` field exists on cards, so CardScreen still shows a single generic placeholder category icon. When a category taxonomy is designed, add the field + per-category icons and map it in CardScreen._populate().
- **2026-06-29** (Save/Persistence System — RESOLVED, found via manual playtesting): `SaveSystem.mark_dirty()` had ZERO call sites anywhere outside `save_system.gd` itself — ADR-0002 designed the wiring `[mutation] -> mark_dirty() -> debounce` but it was never implemented, so `save.json` was never written during real gameplay and nothing survived an app restart. Fixed by wiring `mark_dirty()` into the three real mutation funnels: `ResourceManager.apply_delta()` (guarded on non-empty deltas), `HistoryFlagManager.set_milestone()`, `HistoryFlagManager.increment_counter()` (guarded on the existing non-negative check). Verified via 2 real headless cold-start runs (save.json now created after boot, second boot restores + continues offline accrual correctly). 5 new regression tests. Code review: godot-gdscript-specialist CLEAN.
- **2026-07-13** (Class Path System, Story 002 — RESOLVED during code review, not shipped broken): `ClassPathSystem.restore_state()`/`serialize_state()` originally round-tripped only the derived `_affiliation` total, never the new `_card_contribution`/`_investment_contribution` terms Story 002 introduced. A save/load followed by any card resolution on that path would silently recompute `_affiliation` from a zeroed `_investment_contribution`, discarding all prior investment (resource already spent, progress erased with no error). Found by godot-gdscript-specialist during `/code-review`; fixed before merge — both terms now persist. 2 regression tests added (`test_investment_contribution_survives_save_load_round_trip`, `test_card_contribution_survives_save_load_for_invest_gate`). — tracked from `production/epics/class-path-full/story-002-investment-contribution.md`
- **2026-07-13** (Class Path System, Story 002): `_INVESTMENT_RATE_TABLE` is a GDScript const, not loaded from `assets/data/balance.json` as ADR-0010 §8 aspirationally describes — matches the already-shipped `_MULTIPLIER_TABLE`/`CARD_AFFILIATION_PER_CHOICE` pattern (no `balance.json` exists in this project yet). Same project-wide tech debt as the `CARD_AFFILIATION_PER_CHOICE` entry (2026-06-23 equivalent, resource-system) — extraction to a real data file remains a standing, not-yet-scheduled item. — tracked from `production/epics/class-path-full/story-002-investment-contribution.md`
