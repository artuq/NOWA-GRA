# Godot 4.x
.godot/
*.translation
export_presets.cfg

# GdUnit4 test reports (regenerated on every run)
reports/

# macOS
.DS_Store

# Agent working memory (scratch, not a project deliverable)
.claude/agent-memory/

# Session audit trail and ephemeral session state (per directory-structure.md
# — these are local working artifacts, not project deliverables)
production/session-logs/
production/session-state/
