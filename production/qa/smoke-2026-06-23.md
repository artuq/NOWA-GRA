# Smoke Check Report

**Date**: 2026-06-23
**Sprint**: 1 (Resource System)
**Engine**: Godot 4.6.3 (ran on 4.6.2 stable binary)
**QA Plan**: production/qa/qa-plan-sprint-1-2026-06-20.md
**Argument**: sprint

---

## Automated Tests

**Status**: PASS — 55 tests, 55 passing, 0 failures, 0 flaky, 0 orphans

Run via `addons/gdUnit4/runtest.sh -a tests/unit` against a real Godot engine, exit code 0.

| Suite | Tests |
|-------|-------|
| core_mutation_test.gd | 7 |
| haters_growth_rate_test.gd | 6 |
| morale_drain_rate_test.gd | 8 |
| effectiveness_multiplier_test.gd | 12 |
| passive_income_test.gd | 11 |
| cringe_clamping_test.gd | 7 |
| sponsors_acquisition_test.gd | 4 |
| **Total** | **55** |

---

## Test Coverage

| Story | Type | Test File | Coverage |
|-------|------|-----------|----------|
| 1-1 Core Resource Mutation | Logic | core_mutation_test.gd | COVERED |
| 1-2 Hatersi Growth (A) | Logic | haters_growth_rate_test.gd | COVERED |
| 1-3 Morale Drain (B) | Logic | morale_drain_rate_test.gd | COVERED |
| 1-4 Effectiveness Multiplier (C) | Logic | effectiveness_multiplier_test.gd | COVERED |
| 1-5 Passive Income (D) | Logic | passive_income_test.gd | COVERED |
| 1-6 Cringe Clamping (E) | Logic | cringe_clamping_test.gd | COVERED |
| 1-7 Sponsors Acquisition | Config/Data | sponsors_acquisition_test.gd | COVERED |

**Summary**: 7 covered, 0 manual, 0 missing, 0 expected-without-test. All 7 sprint stories have passing automated coverage.

---

## Manual Smoke Checks

- [x] Code compiles and all scripts + autoloads load without parse errors — PASS (GdUnit4 successfully parsed and ran 55 tests; `ResourceManager` autoload script loads clean)
- [-] Play-based checks (game launch, scene navigation, touch input, 3 actions, decision cards, save/load, offline report) — **N/A this sprint**

The smoke checklist at `tests/smoke/critical-paths.md` describes the full game loop, but none of those systems exist in production `src/` yet. Production `src/` contains only `resource_formulas.gd` + `resource_manager.gd` (the only `.tscn` files in the repo belong to the GdUnit4 test addon). Sprint 1 was scoped as backend foundation only — there is no playable production surface to manually exercise.

---

## Scope Note (Product-Owner Flag)

During the manual-check step, the product owner flagged that they **expected a playable build** from sprint 1. This is a genuine expectations gap, not a code defect: sprint 1's goal was explicitly "implement Resource System's core mutation mechanism and all 5 formulas (A-E) in src/, with passing unit tests" — i.e. the foundational backend layer that later systems build on.

Resolution during this session:
- The playable **vertical-slice prototype** (`prototypes/krol-cringeu-vertical-slice/`) was launched live in Godot — it booted cleanly (no parse/autoload errors) and demonstrates the full game loop (5 systems), but it is throwaway pre-production code, isolated from `src/`.
- Sprint 1 rebuilt the **first** of those five systems (Resource System) as production-quality, tested code.
- **Roadmap decision (2026-06-23)**: continue backend-first, rebuilding systems one at a time into `src/`. Next production system: **Action System** (governed by ADR-0004, depends on the now-complete Resource System).

This item belongs to sprint planning / roadmap, not to the QA hand-off gate.

---

## Missing Test Evidence

None — all Logic stories have passing test files; the Config/Data story (1-7) exceeded its smoke-only requirement with a deterministic unit test.

---

## Verdict: PASS (backend scope)

Automated tests pass cleanly (55/55), all stories are covered, and the code loads without error. The delivered scope — Resource System backend — is sound and fully verified.

There is no playable production surface to hand to manual QA this sprint (by design); the applicable QA for backend logic (automated suite + code review) is already complete. The "expected a playable build" flag is recorded as a roadmap item, not a smoke-check failure.

**Note on QA infrastructure**: subagent spawns were hitting transient HTTP 500s on the sonnet model during this session (diagnosed as a server-side incident, not a project/plugin defect — see `docs/tech-debt-register.md`). This smoke-check runs in the main session and was unaffected. Subsequent subagent-based steps (`/team-qa`, `/retrospective`) may need a `model: opus` override until sonnet recovers.
